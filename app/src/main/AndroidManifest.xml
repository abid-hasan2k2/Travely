<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
<uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/travely_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/travely_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Travely"
        tools:targetApi="31">
        <activity android:name=".MountainActivity" />
        <activity android:name=".SeaActivity" />
        <activity android:name=".WaterFallActivity" />
        <activity android:name=".TeaGardenActivity" />
        <activity android:name=".MuseumActivity" />
        <activity android:name=".RiverActivity" />
        <activity android:name=".ParkActivity" />
        <activity android:name=".MosqueActivity" />
        <activity android:name=".TempleActivity" />
        <activity android:name=".ForestActivity" />
        <activity android:name=".EventFormActivity" />
        <activity android:name=".ExpenseActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>